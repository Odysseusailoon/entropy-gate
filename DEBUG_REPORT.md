# Entropy gate 净收益实验：调试报告与 8B 上线条件

日期：2026-09-07。范围：代码审查、本地测试，以及 Qwen3-1.7B / A100 开发题调试。

**四种方法的采样、共享缓存、树权重和成本记录已经跑通；真实模型的开发预检仍未通过，因此没有启动正式成本标定、测试或 reference 队列。** 本轮不提供 entropy 的净收益结论。正式配置仍为 Qwen3-8B / H200；1.7B 的结果只用于发现实现问题。

## 1. 本轮实际做了什么

- 服务器实际是 8 张 A100-PCIE-40GB，并非 H200。本轮仅使用物理 GPU 0，任务已结束；没有停止其他人的 GPU 进程。
- 在 `/mnt/nvme1/entropy-gate-net-yifei-20260907` 建立独立目录和环境，复用已有 1.7B 权重，未下载 8B，未修改共享 Python 环境。
- 模型和 tokenizer 固定到 revision `70d244cc86ccca08cf5af4e1e306ecf908b1ad5e`；GSM8K 固定到 `740312add88f781978c0658806c59bc2815b9866`。
- 使用 non-thinking、BF16、SDPA、batch 上限 8、完整 softmax；四种方法均使用 KV cache 和共享 prompt cache。
- 在一道开发题上完成四种方法各 16 条路径的 GPU 检查；另对 5 道开发题完成每题 10 条 IID 预检。测试题未用于调参或生成。

## 2. 最值得在 8B 上提前排查的问题

### A. 增大长度上限，不一定能消除截断

50 条开发 rollout 的两次结果如下。第二次保留相同问题和随机种子，只把 1.7B 调试配置的保护上限从 1024 提高到 4096。

| 结果 | 上限 1024 | 上限 4096 |
|---|---:|---:|
| EOS 结束且可解析 | 47 | 47 |
| EOS 结束但无法解析 | 2 | 2 |
| 到达长度上限 | 1 | 1 |

同一条长轨迹（`train-11`，`review_id=39`）在 4096 tokens 时仍在重复推导。其前 1024 个 token 与第一次完全一致，不能靠提高到 4096 就认定问题已解决。无法解析率为 4%，未超过当前 5% 门槛；**截断率为 2%，违反零观测截断要求，整体预检失败。**

**8B 启示：**先在 8B 自己的开发题上检查长度尾部，再冻结保护上限。当前正式配置中的 1024 尚未得到 8B 实测验证。不得删除长轨迹、只统计及时结束的答案，或反复重抽直到预检通过。若仍出现截断，应保留记录并停止晋级；将“到达长度上限”直接当普通答案计分，会改变这里要求的 EOS 答案分布目标。即使 50 条全部结束，也只能说明这次未观察到截断，后续标定、测试和 reference 仍需检查。

原始记录：[50 条 rollout](runs/a100-preflight/net-qwen17b-a100-v2/preflight_rollouts.jsonl)、[长轨迹原文](runs/a100-preflight/net-qwen17b-a100-v2/long_rollout_39.txt)、[预检结果](runs/a100-preflight/net-qwen17b-a100-v2/preflight.json)。

### B. Entropy 分数不同，最终整数分配仍可能完全相同

一道开发题、8 个独立前缀、总计 16 条路径、`beta=1` 时，各前缀 entropy 分数约为 0.065–0.146 bits，但取整后全部获得 2 条续写。因此该次 entropy 与 uniform 的分配、样本和答案分布完全相同，entropy 只增加了 gate 计算。

**8B 启示：**开发检查必须同时查看 entropy 分数、每个前缀的实际子分支数，以及与 uniform 的分配差异。仅确认 entropy 被计算了还不够。本次没有据此改动 beta，也不能据这一道题判断 entropy 无效；如需改参数，必须在正式测试前完成并重新冻结。只赢 IID，不能证明 entropy 的额外贡献。

### C. BF16 跨 batch 不逐 token 相等，不等于随机数实现有错

真实 GPU 上，batch=1 与 batch=8 的微小数值差异可能改变某次抽样，从而让后续整条路径分离。原先把跨 batch 路径完全相等作为硬性门槛，会误报。

**已修复：**同配置、同随机种子的计时运行与 FLOPs 重放仍必须严格匹配 token、权重、分配和执行记录；跨 batch 路径相等改为诊断项，并新增相同固定前缀的下一 token 概率比较。三个位置的最大 TV 为 `2.8934e-7`，通过预设 `0.01` 门槛。四种方法的同配置重放均通过。

**8B 启示：**在 8B/H200 的实际精度、attention 实现和 batch 条件下重做检查，reference 使用一致的采样条件。这三个局部探针不是完整答案分布的误差界，也不能证明所有 batch 差异都可忽略。

### D. Arithmetic Sampling 的长序列数值精度需要单独处理

反复使用 float64 更新 arithmetic residual，会逐步耗尽初始随机码的有效精度。只在短序列上检查，可能漏掉长轨迹中的退化。

**已修复：**使用按长度与词表大小确定精度的共同随机平移，并用精确有理数更新 residual、校正 CDF 边界。相关长序列测试及真实 GPU 的完整采样和重放已通过。

**8B 启示：**精确更新带来的 CPU 大整数计算、数据复制和同步时间必须算入 GPU 占用时间。目前是该算法在本项目统一采样框架中的实现；本次计时不能代表作者优化系统的吞吐率。

### E. 预检通过后，开发标定仍可能遇到新的长轨迹

最终代码检查发现：原来的预算封存只检查记录身份与 FLOPs 覆盖，没有再次检查开发标定中的截断和无法解析质量，可能把长度上限缩短的运行成本用于正式预算。

**已修复：**封存预算前，按方法汇总每次独立运行的加权 invalid / truncated 概率质量，并应用与正式分析一致的质量门槛。默认零截断规则下，只要标定中出现正的截断质量，就拒绝写入预算计划及其封存文件；原始样本和已花费成本保留。成功封存时同时记录开发质量摘要。

新增回归测试覆盖“仅一条标定运行截断”和“无法解析质量超阈值”两种失败，检查不会产生计划，也不会改写原始记录。此修正仅做本地验证，未重新消耗服务器 GPU。

### F. 环境和离线缓存可能让实验身份悄悄变化

本轮遇到三个实际问题：共享环境与项目环境的 Transformers 版本不同；Torch 导入后触发 C++ 运行库不兼容；服务器无法联网时，数据加载器可能退回其他缓存版本。

**已修复：**项目独立环境固定 Transformers 4.57.6；只为本任务进程指定可用的 C++ 库路径；版本记录读取当前生效环境；离线数据缓存必须匹配请求的 revision。模型参数量从真实结构读取，确认调试模型为 1,720,574,976 参数。

**8B 启示：**权重、tokenizer、数据、库版本及 GPU 型号都要在正式机器上重新确认并冻结。不能把本轮 1.7B/A100 的封存文件当成 8B/H200 协议。

## 3. 估计器与成本口径核对

分叉实现保留原模型续写概率，没有强制替换 token。独立前缀数量预先确定；分配只使用已生成前缀的信息。根分支权重为 `1/R`，其 `k` 条子分支各继承 `1/(Rk)`。因此分叉更多的前缀不会获得更多总概率质量。已用已知答案分布的测试检查这一点。

共享前缀实际复用 KV 状态；可写续写缓存各自复制，复制开销计入时间。IID 同样只计算一次共享 prompt，使用正常 KV cache。四种方法均完成预定路径数，不按墙钟截止时间筛选已结束答案。

成本现在采用以下口径：

- **FLOPs：**根据实际执行的浮点算子及形状计数，FMA=2，标量超越函数按约定计数；未覆盖浮点算子使检查失败。它是执行算子计数，**不是硬件指令计数**。内存复制、整数操作等不计作浮点运算，但占用时间照计。
- **GPU 秒数：**单张 GPU 被完整算法占用的同步墙钟时间，包含前缀、续写、gate、采样、缓存复制、Python 调度、解析与加权统计，不只是 CUDA kernel 活跃时间。
- **开发开销：**正式净成本分析按预注册测试规模摊入开发标定和被丢弃的 worker warmup；当前单题 debug 表没有该摊销。reference、通用正确性预检、模型加载和 FLOPs 测量重放属于单列的实验基础开销，不纳入方法主成本。因而主图不等于整个研究作业的总 GPU 账单。
- **测量干扰：**主时间来自无 FLOPs 计数器的运行；随后精确重放统计 FLOPs，严格检查两次执行一致。重放耗时单独记录，不混入方法速度。

**当前仍不是每个测试运行的实测 FLOPs 严格相等。** 开发阶段按实测 FLOPs 选择接近 IID 16/32/64 预算的固定路径数；测试时完整执行并记录实际成本，因此不同题目和方法的实际成本允许不同。分析使用真实观测点，不再使用共同区间插值，也不进行外推。阈值成本取已测试配置中达到误差阈值的最低观测成本，因此受预算网格粗细限制。

置信区间在题目与完整独立重复运行层面做配对重采样，同一次重采样对四种方法使用相同索引；共享前缀的叶子不当成独立样本。

## 4. 实测工程证据，不是净收益排名

下面来自同一道开发题、每组 16 条预定路径的首次完整检查；64 条路径全部 EOS、全部可解析，每组一次 prompt prefill，浮点算子覆盖检查全部通过。

| 方法 | 执行算子 FLOPs（万亿次） | GPU 占用秒数 |
|---|---:|---:|
| IID full rollout | 10.779 | 18.630 |
| 共享前缀 + uniform | 8.632 | 13.772 |
| 共享前缀 + entropy | 8.633 | 13.800 |
| Arithmetic Sampling | 10.427 | 13.779 |

没有独立 reference，也没有足够的题目和重复运行，所以不能据此声称某方法达到相同误差更快，更不能给出正式 speedup。原始精确数值见 [成本记录](runs/a100-debug/qwen17b-a100-01/measured_debug_costs.csv)。

另有完整合成队列演练，覆盖预算封存、配对任务、reference、误差分析和两张图的生成；合成误差与 CPU 耗时均不作为真实 GPU 结果。

最终本地完整测试 **65 项通过（43.90 秒）**，包含新增的两项标定质量回归测试。远端此前新增的 6 项小型真实 Qwen3 CPU 测试也已通过；最后的标定保护修正未在远端重新运行。实际 1.7B GPU 检查与 50 条预检的证据边界如上，不能用单元测试通过替代正式模型验证。

## 5. 8B 正式实验开始前的顺序

1. 在实际 8B/H200 环境重复四方法的小规模检查：KV 复用、父缓存不被改写、权重、同配置重放、浮点算子覆盖，以及显存峰值。A100/1.7B 的通过不能替代这一步。
2. 用独立开发题检查完整生成的长度尾部与至少 50 条答案解析；人工审查尚未在本轮代签。完成 gate 分数和整数分配检查，再冻结所有参数。出现长循环时先解决协议或实现问题，不进入正式队列。
3. 执行开发成本标定。新增质量检查通过后才能封存预算；测试过程中完整完成预定运行，继续检查截断和解析质量。
4. 按正式配置运行 50 道未用于调参的测试题、16/32/64 三档预算、10 次独立重复；从每题 1024 条独立 reference 开始检查稳定性。五张空闲 H200 共用任务队列，四方法配对块在同一张卡完成。
5. 只在 reference 与质量检查通过后生成 FLOPs–误差、GPU 秒数–误差两张主图，以及预设阈值的配对置信区间。优先判断 entropy 是否胜过 uniform，再解释相对 IID 与 Arithmetic Sampling 的净收益。

**交付状态：工程修正已落地，正式实验尚未启动；长循环问题仍需在 8B 开发阶段验证和处理。** 当前正式参数见 [配置](configs/net-benefit.yaml)，详细历史调试记录见 [VALIDATION](runs/a100-debug/VALIDATION.md)。
